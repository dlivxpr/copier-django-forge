services:
  db:
    image: postgres:15-alpine
    container_name: {{project_slug}}-dev-db
    env_file: 
      - ../.env
    environment:
      POSTGRES_DB: ${DB_NAME:-{{project_slug}}}
      POSTGRES_USER: ${DB_USER:-{{project_slug}}}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-{{project_slug}}}
    ports:
      - "5432:5432"   # 本机连接: localhost:5432
    volumes:
      - {{project_slug}}_db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-{{project_slug}}} -d ${DB_NAME:-{{project_slug}}}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - {{project_slug}}-network

  redis:
    image: redis:7.4.5-alpine
    container_name: {{project_slug}}-dev-redis
    env_file:
      - ../.env
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"   # 本机连接: localhost:6379
    volumes:
      - {{project_slug}}_redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - {{project_slug}}-network

volumes:
  {{project_slug}}_db:
  {{project_slug}}_redis:

networks:
  {{project_slug}}-network:
    driver: bridge
